name: Legacy schedule keepalive (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  information:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Cloudflare does not require repository keepalive commits."
