name: Legacy Telegram sender (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  information:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Delivery is managed by Cloudflare Workers. See cloudflare/README.md."
